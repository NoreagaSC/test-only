import React, { type FC, type ReactElement } from 'react';

import { StyledTitleLayout } from './title.styles';

export const Title: FC = (): ReactElement => {
  return <StyledTitleLayout>Исторические даты</StyledTitleLayout>;
};
