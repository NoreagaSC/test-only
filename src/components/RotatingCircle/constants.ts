import { DOTS_COUNT } from 'shared';

export const CIRCLE_SIZE = 530;
export const RADIUS = CIRCLE_SIZE / 2;

export const ANGLE_STEP = (2 * Math.PI) / DOTS_COUNT;

export const FIXED_ACTIVE_INDEX = 0;

export const BASE_ROTATION_STEP = 360 / DOTS_COUNT;
